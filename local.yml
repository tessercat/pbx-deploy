---
- name: Deploy peers
  hosts: 127.0.0.1
  connection: local
  become: false
  gather_facts: false
  vars:

      web_repo: https://github.com/tessercat/peers-web.git
      web_version: master
      web_port: 8900

  roles:
      - {role: web, tags: [web]}
